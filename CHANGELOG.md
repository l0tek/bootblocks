# Changelog

## Unreleased
- Rebuilt header/footer template parts with valid block markup.
- Added container vs. container-fluid page templates and registered them in `theme.json`.
- Added separate header variants to match template width (`header-container` / `header-fluid`).
- Implemented sticky header behavior.
- Added carousel height control and fixed consistent slide sizing.
- Restored BootBlocks custom blocks and editor assets.
- Hardened carousel output via server-rendered block to avoid invalid block states.
- Cleaned templates to avoid invalid block wrapper structures.
